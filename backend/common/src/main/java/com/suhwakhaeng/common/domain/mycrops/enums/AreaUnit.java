package com.suhwakhaeng.common.domain.mycrops.enums;

public enum AreaUnit {
    // 평방미터, 평, 헥타르 >> 기본은 평방미트(제곱미터) >> 향후 단위랑 들어오는 값에 따라서 단위 통일시키도록 변경해서 저장
    // 평방미터(그대로 유지), 평(퍙 -> 평방미터 계산식), 헥타르(헥타르 -> 평방미터 계산식)
    
    // 이름에 맞는 enum 값 반환해주는 메소드
}
