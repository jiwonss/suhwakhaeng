pipeline {
    environment {
        repository = "sjs3326/suhwakhaeng"
        DOCKERHUB_CREDENTIALS = credentials('dockerhub_token')
        dockerImage = ''
    }

    agent any

    stages {
        stage('Clone Repository') {
            steps {
                checkout scm
            }
        }
        stage('Build Project') {
            steps {
                dir("./backend/service-discovery") {
                    sh "chmod +x ./gradlew"
                    sh "./gradlew clean build"
                }
            }
        }
        stage('Build Discovery-Server Image'){
             steps{
                   script{
                        dockerImage = docker.build("${repository}:service_discovery_${BUILD_NUMBER}", "-f backend/service-discovery/Dockerfile ./backend/service-discovery")
                   }
              }
        }

        stage('DockerHub Login'){
            steps{
                script{
                    sh "echo \${DOCKERHUB_PASSWORD} | docker login -u \${DOCKERHUB_ID} --password-stdin"
                }
            }
        }

        stage('Clean Service Discovery Image'){
            steps{
                script{
                     sh "docker rmi \${repository}:service_discovery_\${BUILD_NUMBER}"
                }
            }
        }

        stage('Push Service Discovery Image'){
            steps{
                script{
                     sh "docker push \${repository}:service_discovery_\${BUILD_NUMBER}"
                }
            }
        }


    }
}
