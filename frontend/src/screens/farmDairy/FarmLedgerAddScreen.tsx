import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const FarmLedgerAddScreen = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>FarmLedgerAddScreen</Text>
    </SafeAreaView>
  );
};

export default FarmLedgerAddScreen;
